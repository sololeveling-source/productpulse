---
phase: 01-foundation-competitor-management
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - pnpm-lock.yaml
  - next.config.ts
  - tsconfig.json
  - postcss.config.mjs
  - eslint.config.mjs
  - components.json
  - .gitignore
  - src/app/layout.tsx
  - src/app/page.tsx
  - src/app/globals.css
  - src/app/competitors/page.tsx
  - src/components/app-sidebar.tsx
  - src/components/ui/* (shadcn copy-in)
autonomous: false
requirements: [COMP-01, COMP-02]
tags: [nextjs, scaffold, tailwind, shadcn, walking-skeleton]

must_haves:
  truths:
    - "Running `npx next dev` serves a dark-themed app shell with a 240px sidebar containing Feed and Competitors nav items"
    - "Navigating to / shows the feed placeholder empty state; /competitors shows the competitors page shell"
    - "package.json pins drizzle-orm 0.45.2 and drizzle-kit 0.31.10 — no @rc versions anywhere"
  artifacts:
    - path: "package.json"
      provides: "Pinned dependency set (next 16.2.10, drizzle-orm 0.45.2, zod 4.4.3, vitest 4.1.9)"
      contains: "\"drizzle-orm\": \"0.45.2\""
    - path: "src/app/layout.tsx"
      provides: "Dark-mode root layout with sidebar shell"
      min_lines: 25
    - path: "src/app/page.tsx"
      provides: "Feed placeholder page with link to /competitors"
    - path: "src/components/ui/dialog.tsx"
      provides: "shadcn dialog primitive (proof shadcn add succeeded)"
  key_links:
    - from: "src/app/layout.tsx"
      to: "src/components/app-sidebar.tsx"
      via: "import + render in body"
      pattern: "app-sidebar"
    - from: "src/components/app-sidebar.tsx"
      to: "/competitors"
      via: "next/link nav item"
      pattern: "href=\"/competitors\""
---

## Phase Goal

**As a** user, **I want to** manage competitors and the URLs to monitor in a deployed, publicly reachable app, **so that** competitor monitoring in later phases has real targets backed by the full data schema.

<objective>
Scaffold the Next.js 16 project into the existing repo, install the full pinned dependency set (gated by a package-legitimacy checkpoint), and build the dark app shell (sidebar, feed placeholder, competitors page shell) per the approved UI-SPEC.

Purpose: Walking Skeleton foundation — every later plan builds inside this scaffold. Deliberately does NOT touch the database; after this plan a user can run the app locally and navigate the shell.
Output: Runnable Next.js app with app shell, all Phase 1 dependencies installed at exact pinned versions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-competitor-management/01-RESEARCH.md
@.planning/phases/01-foundation-competitor-management/01-UI-SPEC.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Approve package install list (legitimacy gate)</name>
  <what-built>Nothing yet — this gate precedes all installs. slopcheck was unavailable during research, so every package is [ASSUMED] per RESEARCH.md Package Legitimacy Audit. Manual signals are strong: all are canonical, multi-year-old, mass-adoption packages from official docs, with official source repos and zero postinstall scripts (verified via npm registry 2026-07-03).</what-built>
  <how-to-verify>
    Review the exact install list below. Spot-check any package at its registry page (npmjs.com/package/NAME):
    1. Direct installs: next@16.2.10, drizzle-orm@0.45.2, @neondatabase/serverless@1.1.0, zod@4.4.3, dotenv@17.4.2, drizzle-kit@0.31.10 (dev), vitest@4.1.9 (dev), tsx@4.22.5 (dev), tailwindcss@4.3.2 (via scaffold)
    2. Pulled by tooling: create-next-app@16.2.10 scaffold brings react/react-dom 19, eslint 9, @tailwindcss/postcss; shadcn@4.12.0 CLI (via pnpm dlx) brings lucide-react, @radix-ui/* primitives, class-variance-authority, tailwind-merge, tw-animate-css; the sonner component brings the sonner package
    3. Registry repos: github.com/vercel/next.js, github.com/drizzle-team/drizzle-orm, github.com/neondatabase/serverless, github.com/colinhacks/zod, github.com/shadcn-ui/ui, github.com/vitest-dev/vitest, github.com/privatenumber/tsx, github.com/motdotla/dotenv
  </how-to-verify>
  <resume-signal>Type "approved" to allow installs, or name any package to reject</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Scaffold Next.js into repo root and install pinned dependencies</name>
  <files>package.json, pnpm-lock.yaml, next.config.ts, tsconfig.json, postcss.config.mjs, eslint.config.mjs, components.json, .gitignore, src/app/* (scaffold), src/components/ui/* (shadcn)</files>
  <read_first>
    - .planning/phases/01-foundation-competitor-management/01-RESEARCH.md (sections: Installation, Pitfall 1 temp-dir workaround, Pitfall 5 rc-version guard, Environment Availability)
    - .planning/phases/01-foundation-competitor-management/01-UI-SPEC.md (Design System table — shadcn init preset: base color zinc, CSS variables yes)
    - .gitignore (if present in repo root — must merge, not overwrite)
  </read_first>
  <action>
    Activate pnpm: run `corepack enable`; if it fails on permissions, prefix every pnpm command below with `corepack` (e.g. `corepack pnpm add ...`) — do NOT `npm i -g pnpm`.

    Scaffold (RESEARCH.md Pitfall 1 — repo root is non-empty, create-next-app will refuse it): run `pnpm create next-app@16.2.10 productpulse-scaffold --ts --tailwind --eslint --app --src-dir --import-alias "@/*" --use-pnpm --disable-git` inside /tmp, then `rsync -a /tmp/productpulse-scaffold/ "<repo-root>/"` and remove the temp dir. If the scaffold's .gitignore would overwrite an existing one, merge them. Ensure `.env*` is gitignored and add an exception line `!.env.example`.

    Install pinned data-layer deps (RESEARCH.md Pitfall 5 — official Drizzle docs currently push @rc; NEVER install @rc): `pnpm add drizzle-orm@0.45.2 @neondatabase/serverless@1.1.0 zod@4.4.3 dotenv@17.4.2` and `pnpm add -D drizzle-kit@0.31.10 vitest@4.1.9 tsx@4.22.5`.

    Initialize shadcn per UI-SPEC preset: `pnpm dlx shadcn@4.12.0 init` with base color zinc, CSS variables yes. Then `pnpm dlx shadcn@4.12.0 add button input label card table dialog alert-dialog badge select sonner tooltip` (the UI-SPEC Component Inventory — includes alert-dialog, sonner, tooltip beyond the RESEARCH install list).

    Add a package.json script `"test": "vitest run"`. Verify package.json afterward: drizzle-orm exactly 0.45.2, drizzle-kit exactly 0.31.10, no dependency version containing "rc".
  </action>
  <verify>
    <automated>test -f src/app/layout.tsx && test -f src/components/ui/dialog.tsx && test -f src/components/ui/alert-dialog.tsx && node -e "const p=require('./package.json');const all={...p.dependencies,...p.devDependencies};if(!/0\.45\.2/.test(all['drizzle-orm']))throw new Error('drizzle-orm not pinned');if(JSON.stringify(all).includes('rc'))throw new Error('rc version detected');console.log('deps ok')"</automated>
  </verify>
  <done>Scaffold lives in repo root (src/app/, package.json), all 11 shadcn components exist under src/components/ui/, pinned versions verified, `.env*` gitignored with `!.env.example` exception, `npx next build` exits 0.</done>
  <acceptance_criteria>
    - `test -f src/components/ui/sonner.tsx` and `test -f src/components/ui/tooltip.tsx` exit 0
    - `node -e` version-guard script above prints "deps ok"
    - `grep -c "^\.env" .gitignore` >= 1 and `grep -c '!\.env\.example' .gitignore` == 1
    - `npx next build` exits 0
    - `/tmp/productpulse-scaffold` no longer exists
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Build the dark app shell per UI-SPEC (sidebar, feed placeholder, competitors shell)</name>
  <files>src/app/layout.tsx, src/app/globals.css, src/app/page.tsx, src/app/competitors/page.tsx, src/components/app-sidebar.tsx</files>
  <read_first>
    - .planning/phases/01-foundation-competitor-management/01-UI-SPEC.md (entire file — Color, Typography, Spacing, App Shell & Layout, Copywriting Contract are LOCKED)
    - src/app/layout.tsx (scaffolded state — Geist font wiring to preserve)
    - src/app/globals.css (scaffolded Tailwind 4 + shadcn zinc variables to override)
  </read_first>
  <action>
    Implement the approved UI-SPEC shell exactly. Dark-only: `<html class="dark">` in layout.tsx, no theme toggle. Keep the scaffolded Geist Sans/Geist Mono font setup.

    globals.css: override shadcn zinc tokens to the UI-SPEC Color table — background #09090B (zinc-950), card/sidebar surfaces #18181B with #27272A 1px borders, primary/accent #22D3EE (cyan-400) with foreground #083344, destructive #EF4444, text primary #FAFAFA, muted text #A1A1AA, focus ring cyan.

    src/components/app-sidebar.tsx: fixed left sidebar, 240px wide, zinc-900 bg, zinc-800 right border, full height. Top: wordmark "ProductPulse" (14px semibold) with lucide-react `Radar` icon at 24px, 24px padding. Nav items (14px, 8px vertical / 16px horizontal padding): "Feed" → href "/", "Competitors" → href "/competitors". Active item (match via usePathname, client component): 2px cyan left indicator bar, cyan label, zinc-800 bg pill. Inactive: zinc-400 label, hover zinc-800 bg.

    layout.tsx: render sidebar + main content area (zinc-950 bg, 24px padding, max-w-[1120px], left-aligned).

    src/app/page.tsx (Feed placeholder, exact copy from UI-SPEC): empty-state layout with 20px heading "Your intelligence feed", 14px muted body "Detected competitor changes will appear here once monitoring starts.", and link text "Manage competitors →" navigating to /competitors via next/link. No dead CTA.

    src/app/competitors/page.tsx (interim shell — Plan 03 replaces this with the DB-backed page): page header row with 28px/600 Display title "Competitors", then the empty state per UI-SPEC copy — heading "No competitors yet", body "Add a competitor and the changelog or pricing pages you want ProductPulse to watch." Do NOT render the "Add competitor" CTA yet (the dialog and server action arrive in Plan 03; no dead buttons).

    Typography discipline: only 12/14/20/28px sizes, weights 400/600. Spacing on the 4px scale per UI-SPEC tokens.
  </action>
  <verify>
    <automated>npx next build && grep -c 'className="dark"\|class="dark"' src/app/layout.tsx | grep -qv '^0$' && grep -q 'href="/competitors"' src/components/app-sidebar.tsx && grep -q "Your intelligence feed" src/app/page.tsx && grep -q "No competitors yet" src/app/competitors/page.tsx</automated>
    <human-check>Run `npx next dev`, open localhost:3000 — dark shell with sidebar, both nav items work, active state shows cyan indicator</human-check>
  </verify>
  <done>App builds and serves the shell: dark theme, 240px sidebar with ProductPulse wordmark + Feed/Competitors nav, feed placeholder with exact UI-SPEC copy, competitors page with empty-state copy. All copy strings match the UI-SPEC Copywriting Contract verbatim.</done>
  <acceptance_criteria>
    - `npx next build` exits 0
    - src/app/layout.tsx contains `class="dark"` (or className) on the html element
    - src/app/page.tsx contains exact strings "Your intelligence feed", "Detected competitor changes will appear here once monitoring starts." and "Manage competitors"
    - src/app/competitors/page.tsx contains exact strings "No competitors yet" and "Add a competitor and the changelog or pricing pages you want ProductPulse to watch."
    - src/components/app-sidebar.tsx contains href="/" and href="/competitors" nav links and the string "ProductPulse"
    - No occurrence of `dangerouslySetInnerHTML` in src/ (`grep -r "dangerouslySetInnerHTML" src/ | wc -l` == 0)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → local machine | Third-party code enters the project at install time |
| Official docs → dependency choices | Drizzle quickstart currently instructs @rc installs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-SC | Tampering | pnpm installs (all packages) | mitigate | slopcheck unavailable → all packages [ASSUMED]; consolidated blocking human checkpoint (Task 1) before any install; exact-version pins; postinstall absence verified in RESEARCH.md |
| T-01-01 | Tampering | drizzle-orm/drizzle-kit versions | mitigate | Explicit @0.45.2/@0.31.10 pins; automated package.json guard rejects any version string containing "rc" |
| T-01-02 | Information Disclosure | .env secrets (future) | mitigate | .gitignore gets `.env*` + `!.env.example` in this plan, before any secret exists |
</threat_model>

<verification>
- `npx next build` exits 0 (Turbopack production build)
- Dependency guard: no "rc" version strings in package.json
- Shell copy strings match UI-SPEC verbatim (grep assertions above)
</verification>

<success_criteria>
- App scaffolded into repo root with pinned dependency set approved via legitimacy checkpoint
- Dark app shell navigable locally: / (feed placeholder) and /competitors (empty-state shell)
- Zero database code — DB work starts in Plan 02
</success_criteria>

<output>
Create `.planning/phases/01-foundation-competitor-management/01-01-SUMMARY.md` when done
</output>
