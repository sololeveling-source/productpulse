---
phase: 01-foundation-competitor-management
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - vitest.config.ts
  - tests/validation.test.ts
  - tests/schema.test.ts
  - src/lib/validation.ts
  - src/lib/db/schema.ts
  - src/lib/db/index.ts
  - drizzle.config.ts
autonomous: true
requirements: [COMP-01, COMP-02]
tags: [drizzle, zod, vitest, schema, tdd, ssrf]

must_haves:
  truths:
    - "`npx vitest run` is green and proves COMP-01 input rules: name required (1–200 chars), at least one URL, kind limited to changelog|pricing, http/https only, internal hosts rejected"
    - "The schema module defines all 5 roadmap tables (competitors, sources, snapshots, changes, digests) and 3 enums, including per-source health fields and published_at/detected_at on changes"
    - "Deleting a competitor cascades to its sources at the schema level (COMP-02 delete semantics)"
  artifacts:
    - path: "src/lib/db/schema.ts"
      provides: "Full roadmap schema — 5 tables + 3 pg enums + composite unique index on (competitor_id, url)"
      min_lines: 60
      contains: "failure_streak"
    - path: "src/lib/validation.ts"
      provides: "zod schemas: urlSchema (scheme allowlist + host denylist), createCompetitorSchema, updateCompetitorSchema"
      exports: ["urlSchema", "createCompetitorSchema", "updateCompetitorSchema"]
    - path: "src/lib/db/index.ts"
      provides: "drizzle neon-http client singleton"
      contains: "neon-http"
    - path: "drizzle.config.ts"
      provides: "drizzle-kit config using DATABASE_URL_UNPOOLED via dotenv"
      contains: "DATABASE_URL_UNPOOLED"
    - path: "tests/validation.test.ts"
      provides: "COMP-01 input rule tests (Wave 0 requirement from 01-VALIDATION.md)"
    - path: "tests/schema.test.ts"
      provides: "Enum values + cascade + unique index assertions (COMP-02 schema semantics)"
  key_links:
    - from: "drizzle.config.ts"
      to: ".env"
      via: "import 'dotenv/config' then process.env.DATABASE_URL_UNPOOLED"
      pattern: "DATABASE_URL_UNPOOLED"
    - from: "src/lib/db/schema.ts"
      to: "competitors table"
      via: "sources.competitor_id FK with onDelete cascade"
      pattern: "onDelete: 'cascade'"
    - from: "tests/validation.test.ts"
      to: "src/lib/validation.ts"
      via: "import { createCompetitorSchema } from '@/lib/validation'"
      pattern: "@/lib/validation"
---

## Phase Goal

**As a** user, **I want to** manage competitors and the URLs to monitor in a deployed, publicly reachable app, **so that** competitor monitoring in later phases has real targets backed by the full data schema.

<objective>
TDD the data contracts of the entire roadmap: zod input validation for competitor CRUD (including the SSRF host-block rules) and the full 5-table Drizzle schema, with the failing tests written first (Wave 0 per 01-VALIDATION.md).

Purpose: Locks enum values, timestamp semantics, health fields, and cascade behavior NOW so Phases 2–6 only ever make additive schema changes (RESEARCH.md Pattern 3 / Pitfall 6). The validation module is the security gate every Server Action will call.
Output: Green vitest suite, schema module, db client, drizzle-kit config. No live database yet — push happens in Plan 03.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-foundation-competitor-management/01-RESEARCH.md
@.planning/phases/01-foundation-competitor-management/01-VALIDATION.md
</context>

<interfaces>
<!-- Contracts this plan CREATES. Downstream plans (03, 04) implement against these exact exports. -->

From src/lib/validation.ts (created here):
- `urlSchema` — zod schema: valid URL, protocol http:/https: only, hostname NOT in denylist (localhost, 127.0.0.1, 169.254.169.254, 0.0.0.0, ::1)
- `monitoredUrlSchema` — z.object({ id: z.number().int().optional(), url: urlSchema, kind: z.enum(['changelog', 'pricing']) })
- `createCompetitorSchema` — z.object({ name: z.string().min(1).max(200), urls: z.array(monitoredUrlSchema).min(1) })
- `updateCompetitorSchema` — createCompetitorSchema extended with id: z.coerce.number().int().positive()

From src/lib/db/schema.ts (created here):
- Enums: `sourceKind` ('changelog'|'pricing'), `fetchStrategy` ('direct'|'jina'), `changeCategory` ('feature_launch'|'pricing_change'|'deprecation'|'fix'|'other')
- Tables: `competitors`, `sources`, `snapshots`, `changes`, `digests` (columns per RESEARCH.md Code Example 1)
- `competitorsRelations` / `sourcesRelations` (drizzle relations for db.query)

From src/lib/db/index.ts (created here):
- `db` — drizzle neon-http client constructed with { client: neon(process.env.DATABASE_URL!), schema }
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing tests for validation rules and schema semantics (RED)</name>
  <files>vitest.config.ts, tests/validation.test.ts, tests/schema.test.ts</files>
  <read_first>
    - .planning/phases/01-foundation-competitor-management/01-VALIDATION.md (Wave 0 requirements, test commands)
    - .planning/phases/01-foundation-competitor-management/01-RESEARCH.md (Code Example 1 schema draft, Pitfall 8 SSRF rules, Open Question 2 composite unique index)
    - tsconfig.json (confirm "@/*" path alias to replicate in vitest resolve.alias)
    - src/app/competitors/page.tsx (UI-SPEC error copy already governs messages; tests assert rule outcomes, not copy)
  </read_first>
  <behavior>
    tests/validation.test.ts (imports from '@/lib/validation' — module does not exist yet, so suite fails RED):
    - createCompetitorSchema rejects: empty name; name of 201 chars; urls: [] (empty array)
    - urlSchema rejects: "ftp://example.com/x", "file:///etc/passwd", "not-a-url"
    - urlSchema rejects internal hosts: "http://localhost:3000/a", "http://127.0.0.1/a", "http://169.254.169.254/latest/meta-data", "http://0.0.0.0/", "http://[::1]/"
    - urlSchema accepts: "https://linear.app/changelog", "http://example.com/pricing"
    - monitoredUrlSchema rejects kind "blog"; accepts kind "changelog" and "pricing"
    - updateCompetitorSchema rejects missing/zero/negative id; coerces string "5" to number 5
    tests/schema.test.ts (imports from '@/lib/db/schema', uses getTableConfig from 'drizzle-orm/pg-core'):
    - sourceKind.enumValues deep-equals ['changelog', 'pricing']
    - changeCategory.enumValues deep-equals ['feature_launch', 'pricing_change', 'deprecation', 'fix', 'other'] (AI-03 verbatim)
    - fetchStrategy.enumValues deep-equals ['direct', 'jina']
    - getTableConfig(sources).foreignKeys: the competitor_id FK has onDelete 'cascade' (COMP-02 delete semantics)
    - getTableConfig(sources).uniqueConstraints or indexes include a unique index over columns competitor_id + url
    - competitors, snapshots, changes, digests table configs exist with expected table names ('competitors', 'snapshots', 'changes', 'digests'); changes has columns published_at and detected_at; sources has columns last_checked_at, last_success_at, last_status, last_error, failure_streak
  </behavior>
  <action>
    Create vitest.config.ts: defineConfig with test.environment 'node', test.include ['tests/**/*.test.ts'], and resolve.alias mapping '@' to './src' (use node:path resolve so '@/lib/validation' imports work — the tsconfig "@/*" alias is not picked up by vitest automatically).

    Write both test files exactly to the behavior list above. Then run `npx vitest run` and confirm the suite FAILS because '@/lib/validation' and '@/lib/db/schema' do not resolve. Commit as `test(01-02): add failing tests for validation rules and schema contracts`.
  </action>
  <verify>
    <automated>test -f vitest.config.ts && test -f tests/validation.test.ts && test -f tests/schema.test.ts && ! npx vitest run</automated>
  </verify>
  <done>Both test files exist, cover every behavior bullet, and `npx vitest run` exits non-zero (RED) solely because the implementation modules don't exist yet.</done>
  <acceptance_criteria>
    - `npx vitest run` exits non-zero (RED state)
    - tests/validation.test.ts contains assertions for all 5 denylisted hosts: localhost, 127.0.0.1, 169.254.169.254, 0.0.0.0, ::1
    - tests/schema.test.ts contains the exact enum arrays ['changelog', 'pricing'] and ['feature_launch', 'pricing_change', 'deprecation', 'fix', 'other']
    - vitest.config.ts contains an alias entry mapping '@' to the src directory
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement validation module, full roadmap schema, db client, drizzle config (GREEN)</name>
  <files>src/lib/validation.ts, src/lib/db/schema.ts, src/lib/db/index.ts, drizzle.config.ts</files>
  <read_first>
    - tests/validation.test.ts and tests/schema.test.ts (created in Task 1 — the executable spec)
    - .planning/phases/01-foundation-competitor-management/01-RESEARCH.md (Code Examples 1 and 2 — the schema draft and db client/config are the source of truth; replicate column names, types, defaults exactly)
  </read_first>
  <behavior>
    - All Task 1 tests pass (GREEN)
    - `npx tsc --noEmit` passes
  </behavior>
  <action>
    src/lib/validation.ts: implement the zod schemas exactly as declared in the interfaces block. urlSchema: z.url() (zod 4) refined so new URL(u).protocol is 'http:' or 'https:' and hostname (lowercased, brackets stripped for IPv6) is not in the denylist ['localhost', '127.0.0.1', '169.254.169.254', '0.0.0.0', '::1'] — refinement message: "URL must be a public http(s) address" (the UI maps failures to UI-SPEC copy; RESEARCH.md Pitfall 8: these stored URLs get fetched server-side in Phase 2, so this is the SSRF gate). If the zod 4.4.3 API differs from RESEARCH.md's example (z.treeifyError etc.), adapt to the actual installed API — the rules, not the call shape, are the contract.

    src/lib/db/schema.ts: transcribe RESEARCH.md Code Example 1 verbatim (5 tables, 3 enums, identity PKs, timezone timestamps, health fields on sources, nullable LLM columns and published_at/detected_at on changes), PLUS two additions: (1) a composite unique index on sources over (competitorId, url) named 'sources_competitor_url_unique' via uniqueIndex from drizzle-orm/pg-core in the table's config callback (locked discretionary decision — prevents duplicate monitoring work in Phase 2); (2) drizzle `relations` definitions — competitorsRelations (competitors has many sources) and sourcesRelations (source belongs to competitor) — so db.query.competitors.findMany({ with: { sources: true } }) works in Plan 03.

    src/lib/db/index.ts: RESEARCH.md Code Example 2 verbatim — neon(process.env.DATABASE_URL!) + drizzle({ client: sql, schema }) from 'drizzle-orm/neon-http', exporting `db`. Server-only module: never import it from a client component (T-01-05).

    drizzle.config.ts at repo root: RESEARCH.md Code Example 2 verbatim — import 'dotenv/config', defineConfig with out './drizzle', schema './src/lib/db/schema.ts', dialect 'postgresql', dbCredentials.url from process.env.DATABASE_URL_UNPOOLED (single .env + dotenv is the locked decision; the unpooled URL is required for schema tooling per RESEARCH.md Pitfall 3).

    Run `npx vitest run` until green. Commit as `feat(01-02): implement validation schemas and full roadmap DB schema`.
  </action>
  <verify>
    <automated>npx vitest run && npx tsc --noEmit && grep -q "DATABASE_URL_UNPOOLED" drizzle.config.ts && grep -q "onDelete: 'cascade'" src/lib/db/schema.ts && grep -c "pgTable(" src/lib/db/schema.ts | grep -q "^5$"</automated>
  </verify>
  <done>Full suite green, typecheck clean, schema defines exactly 5 pgTable calls + 3 pgEnum calls, drizzle.config.ts targets DATABASE_URL_UNPOOLED via dotenv.</done>
  <acceptance_criteria>
    - `npx vitest run` exits 0 with all Task 1 tests passing
    - `npx tsc --noEmit` exits 0
    - `grep -c "pgTable(" src/lib/db/schema.ts` outputs 5; `grep -c "pgEnum(" src/lib/db/schema.ts` outputs 3
    - src/lib/db/schema.ts contains "uniqueIndex" and "sources_competitor_url_unique"
    - src/lib/db/schema.ts contains column names: last_checked_at, last_success_at, last_status, last_error, failure_streak, published_at, detected_at, content_hash
    - drizzle.config.ts contains both "dotenv/config" and "DATABASE_URL_UNPOOLED"
    - src/lib/validation.ts exports urlSchema, monitoredUrlSchema, createCompetitorSchema, updateCompetitorSchema
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Server Action inputs | Untrusted form data crosses here; validation.ts is the gate |
| Stored URL → Phase 2 fetcher | URLs stored now are fetched server-side later (SSRF seed) |
| Repo → .env secrets | drizzle.config.ts reads connection strings from local .env |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02 | Tampering / Info Disclosure | urlSchema (SSRF-by-stored-URL) | mitigate | Scheme allowlist http/https + hostname denylist (localhost, 127.0.0.1, 169.254.169.254, 0.0.0.0, ::1) enforced in zod at input time; covered by tests/validation.test.ts (ASVS V5) |
| T-01-03 | Tampering | SQL injection surface | mitigate | Drizzle parameterized queries only; no raw SQL string interpolation anywhere in src/lib/db |
| T-01-04 | Information Disclosure | DATABASE_URL / DATABASE_URL_UNPOOLED | mitigate | dotenv-loaded .env is gitignored (Plan 01); drizzle.config.ts never logs credentials; db client reads env server-side only |
| T-01-SC | Tampering | npm installs | mitigate | No new installs in this plan (all deps installed in Plan 01 behind the legitimacy checkpoint) |
</threat_model>

<verification>
- `npx vitest run` green (validation + schema suites)
- `npx tsc --noEmit` clean
- Schema contains exactly 5 tables / 3 enums with locked enum values matching COMP-01 (changelog|pricing) and AI-03 (five categories)
</verification>

<success_criteria>
- COMP-01 input rules proven by tests before any UI exists
- COMP-02 delete semantics (FK cascade) asserted at schema level
- Full roadmap schema ready to push — Phases 2–6 need only additive changes
</success_criteria>

<output>
Create `.planning/phases/01-foundation-competitor-management/01-02-SUMMARY.md` when done
</output>
