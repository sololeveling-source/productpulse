---
phase: 01-foundation-competitor-management
plan: 04
type: execute
wave: 4
depends_on: [01-03]
files_modified:
  - src/app/competitors/actions.ts
  - src/components/competitors/competitor-dialog.tsx
  - src/components/competitors/competitor-table.tsx
  - src/components/competitors/delete-competitor-dialog.tsx
autonomous: true
requirements: [COMP-02]
tags: [server-actions, crud, edit, delete, vertical-slice]

must_haves:
  truths:
    - "User can edit a competitor's name and its monitored URLs (add rows, remove rows, change url/kind) and the changes persist (COMP-02)"
    - "User can delete a competitor after a confirmation dialog, and its sources are removed with it (cascade)"
    - "Editing preserves untouched sources' database ids (no delete-and-reinsert wipe) so Phase 2+ snapshot history survives edits"
    - "Delete confirmation uses the exact UI-SPEC copy with 'Delete competitor' / 'Keep competitor' buttons"
  artifacts:
    - path: "src/app/competitors/actions.ts"
      provides: "updateCompetitor (reconcile-by-id) and deleteCompetitor Server Actions"
      contains: "updateCompetitor"
    - path: "src/components/competitors/delete-competitor-dialog.tsx"
      provides: "AlertDialog confirm per UI-SPEC destructive copy"
      contains: "Keep competitor"
    - path: "src/components/competitors/competitor-table.tsx"
      provides: "Actions column with edit (pencil) and delete (trash-2) icon buttons + tooltips + aria-labels"
  key_links:
    - from: "src/components/competitors/competitor-dialog.tsx"
      to: "src/app/competitors/actions.ts"
      via: "edit mode submits to updateCompetitor via useActionState"
      pattern: "updateCompetitor"
    - from: "src/components/competitors/delete-competitor-dialog.tsx"
      to: "src/app/competitors/actions.ts"
      via: "form action={deleteCompetitor} on confirm"
      pattern: "deleteCompetitor"
    - from: "src/app/competitors/actions.ts"
      to: "src/lib/validation.ts"
      via: "updateCompetitorSchema.safeParse"
      pattern: "updateCompetitorSchema"
---

## Phase Goal

**As a** user, **I want to** manage competitors and the URLs to monitor in a deployed, publicly reachable app, **so that** competitor monitoring in later phases has real targets backed by the full data schema.

<objective>
Complete competitor management: edit a competitor (name + URL set, reconciled by source id) and delete a competitor behind the UI-SPEC confirmation dialog. Closes COMP-02 locally.

Purpose: After this plan the user fully manages competitors — add, edit, remove — which is the entire interactive surface Phase 1 promises.
Output: updateCompetitor/deleteCompetitor Server Actions, edit mode on the existing dialog, delete AlertDialog, wired table action buttons.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-competitor-management/01-UI-SPEC.md
@.planning/phases/01-foundation-competitor-management/01-03-SUMMARY.md
</context>

<interfaces>
<!-- Consumed from Plans 02–03 (on disk — use directly). -->

From src/lib/validation.ts:
- `updateCompetitorSchema` — createCompetitorSchema + id: z.coerce.number().int().positive(); each urls[] entry may carry an optional integer `id` (existing source) or none (new source)

From src/lib/db/schema.ts:
- `sources.competitorId` FK has onDelete cascade → deleting a competitor deletes its sources at the DB level
- `snapshots.sourceId` FK cascades from sources → deleting a SOURCE deletes its future snapshots (why edit must reconcile, not recreate)

From Plan 03:
- `CompetitorDialog` — built with mode-ready props; this plan implements mode: 'edit'
- `listCompetitorsWithSources()` returns rows shaped { id, name, createdAt, sources: [{ id, url, kind, ... }] }
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: updateCompetitor (reconcile-by-id) and deleteCompetitor Server Actions</name>
  <files>src/app/competitors/actions.ts</files>
  <read_first>
    - src/app/competitors/actions.ts (current state — createCompetitor pattern to mirror: safeParse, error shape, revalidatePath)
    - src/lib/validation.ts (updateCompetitorSchema shape — id semantics on url rows)
    - src/lib/db/schema.ts (cascade chain sources→snapshots that makes delete-and-reinsert destructive)
    - tests/validation.test.ts (updateCompetitorSchema behaviors already covered — do not duplicate)
  </read_first>
  <behavior>
    - updateCompetitor with a valid payload updates competitors.name and updated_at, updates url/kind on rows carrying an existing source id, inserts rows without an id, and deletes sources whose ids exist in the DB for that competitor but are absent from the payload
    - updateCompetitor rejects invalid ids and all COMP-01 URL rules (same zod gate)
    - deleteCompetitor with a valid id removes the competitor row; sources vanish via FK cascade (no explicit source delete statement)
    - Both actions call revalidatePath('/competitors') on success
  </behavior>
  <action>
    In src/app/competitors/actions.ts add two 'use server' exports.

    updateCompetitor(_prev, formData): parse { id, name, urls } via updateCompetitorSchema.safeParse (urls arrive as the same hidden JSON input; each row may include a source id). CRITICAL reconcile-by-id strategy — do NOT delete all sources and re-insert (snapshots FK-cascade from sources; a recreate would silently wipe Phase 2+ history): (1) update competitors set name, updatedAt=now() where id; (2) select existing source ids for the competitor; (3) for payload rows with an id in that set → update url + kind; (4) for payload rows without id → insert with competitorId; (5) delete sources whose id is in the existing set but not in the payload. Scope every statement by competitorId as well as source id so a forged source id belonging to another competitor cannot be touched. De-duplicate payload urls to respect the composite unique index. Same error-shape contract as createCompetitor ({ error } field-keyed / { formError: 'save_failed' }); return { success: true } on success.

    deleteCompetitor(formData): validate id with z.coerce.number().int().positive() (import zod directly), db.delete(competitors).where(eq(competitors.id, id)) — sources cascade per schema — then revalidatePath('/competitors'). Designed to be used as a plain form action from the AlertDialog confirm button.
  </action>
  <verify>
    <automated>npx vitest run && npx tsc --noEmit && grep -q "updateCompetitorSchema" src/app/competitors/actions.ts && grep -c "revalidatePath('/competitors')" src/app/competitors/actions.ts | grep -qv '^[01]$'</automated>
  </verify>
  <done>Both actions exported with the reconcile-by-id strategy, zod-gated, competitor-scoped, revalidating the list path. Suite and typecheck green.</done>
  <acceptance_criteria>
    - `npx vitest run` and `npx tsc --noEmit` exit 0
    - src/app/competitors/actions.ts exports updateCompetitor and deleteCompetitor and contains at least 3 occurrences of revalidatePath('/competitors') (create + update + delete)
    - updateCompetitor contains no statement deleting ALL sources for a competitor unconditionally (reconcile: the delete uses a not-in-payload id filter)
    - deleteCompetitor contains exactly one db.delete targeting competitors and none targeting sources (cascade does that)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Edit dialog mode, delete confirmation dialog, table action buttons</name>
  <files>src/components/competitors/competitor-dialog.tsx, src/components/competitors/delete-competitor-dialog.tsx, src/components/competitors/competitor-table.tsx</files>
  <read_first>
    - .planning/phases/01-foundation-competitor-management/01-UI-SPEC.md (Copywriting Contract destructive-confirmation row, Competitor list contract aria-label/tooltip rules, Interaction States)
    - src/components/competitors/competitor-dialog.tsx (current add-mode implementation — extend, don't fork)
    - src/components/competitors/competitor-table.tsx (Actions column placeholder from Plan 03)
    - src/components/ui/alert-dialog.tsx and src/components/ui/tooltip.tsx (shadcn APIs)
  </read_first>
  <action>
    competitor-dialog.tsx: implement edit mode. When passed a competitor, prefill name and one URL row per existing source (carrying the source id through the serialized JSON so the action can reconcile). Submit button label "Save changes" (accent fill), action = updateCompetitor. Success toast "Changes saved". Everything else (row add/remove, validation copy, pending Loader2, X-only dismissal) identical to add mode. Removing a URL row in edit mode is reversible until "Save changes" is pressed (no per-row confirmation, per UI-SPEC).

    delete-competitor-dialog.tsx ('use client'): shadcn AlertDialog with EXACT UI-SPEC copy — title "Delete {name}?", body "This removes its monitored URLs and everything ProductPulse has captured for them. This can't be undone.", confirm button destructive fill "Delete competitor" wired to deleteCompetitor via a form action (include hidden id input), dismiss button secondary/outline "Keep competitor". Success toast "Competitor deleted".

    competitor-table.tsx: fill the Actions column — right-aligned ghost icon buttons: edit (lucide Pencil, aria-label "Edit {name}", Tooltip "Edit") opening the dialog in edit mode with that row's competitor; delete (lucide Trash2, destructive hover tint, aria-label "Delete {name}", Tooltip "Delete") opening the AlertDialog. Buttons always visible (no hover-reveal, per UI-SPEC).
  </action>
  <verify>
    <automated>npx vitest run && npx next build && grep -q "Save changes" src/components/competitors/competitor-dialog.tsx && grep -q "Keep competitor" src/components/competitors/delete-competitor-dialog.tsx && grep -q "Delete competitor" src/components/competitors/delete-competitor-dialog.tsx</automated>
    <human-check>`npx next dev`: edit a competitor — rename, change a URL's kind, remove one URL, add another → "Changes saved" toast, table reflects all four; reload → persisted. Delete a competitor → confirm dialog copy matches spec → "Competitor deleted", row and its URLs gone; reload → still gone</human-check>
  </verify>
  <done>Edit and delete flows work end-to-end against Neon with exact UI-SPEC copy, aria-labels, and tooltips; cascade removes URLs with their competitor.</done>
  <acceptance_criteria>
    - `npx vitest run` and `npx next build` exit 0
    - delete-competitor-dialog.tsx contains exact strings "This removes its monitored URLs and everything ProductPulse has captured for them. This can't be undone.", "Delete competitor", "Keep competitor"
    - competitor-dialog.tsx contains "Save changes" and "Changes saved"; contains no standalone "Save", "Cancel", "Submit", "OK" button labels
    - competitor-table.tsx contains aria-label attributes for both edit and delete buttons and renders Pencil and Trash2 icons
    - `grep -r "dangerouslySetInnerHTML" src/ | wc -l` outputs 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → update/delete Server Actions | Publicly invocable POST endpoints mutating and destroying data (no auth by design) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-08 | Tampering | updateCompetitor forged source ids (IDOR across competitors) | mitigate | Every source update/delete statement is scoped by BOTH source id AND competitorId; ids validated as positive integers via zod |
| T-01-09 | Elevation of Privilege | Public deleteCompetitor (anyone can destroy data) | accept | Explicit project decision (auth out of scope, ASVS V2/V4 waived for single-user demo); confirmation UI prevents accidental loss; documented in README (Plan 05) |
| T-01-02 | Tampering / Info Disclosure | Edited URLs (SSRF gate must hold on update path too) | mitigate | updateCompetitorSchema reuses the same urlSchema scheme-allowlist + host-denylist as create |
| T-01-06 | Tampering | Stored XSS via edited name in dialog/table/toast | mitigate | React escaping; interpolate {name} as text nodes only; no dangerouslySetInnerHTML (grep-gated) |
</threat_model>

<verification>
- `npx vitest run` + `npx tsc --noEmit` + `npx next build` green
- Manual: edit persists all four mutation kinds (rename, re-kind, remove row, add row); delete cascades URLs; both survive reload
</verification>

<success_criteria>
- COMP-02 satisfied locally: edit and remove competitors and their monitored URLs
- Reconcile-by-id editing preserves source ids (protects Phase 2+ snapshot history)
- All destructive UI matches UI-SPEC copy exactly
</success_criteria>

<output>
Create `.planning/phases/01-foundation-competitor-management/01-04-SUMMARY.md` when done
</output>
