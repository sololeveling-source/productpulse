import type {
  CreateCompetitorState,
  UpdateCompetitorState,
} from '@/app/competitors/actions'

export const initialCreateCompetitorState: CreateCompetitorState = {
  success: false,
}

export const initialUpdateCompetitorState: UpdateCompetitorState = {
  success: false,
}
